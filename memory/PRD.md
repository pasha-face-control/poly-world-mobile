# HexTribes — Polytopia-style 4X (Expo/React Native)

## Original Problem
Build a game similar to The Battle of Polytopia. Client-side, local persistence.

## Architecture
- Pure client-side game. Engine in `/app/frontend/src/game/` (types, data, rng, grid, combat, mapgen, factory, engine, ai, store). UI in `/app/frontend/src/components/`. Screens in `/app/frontend/app/`.
- 2.5D isometric SVG map (diamond tiles, pyramid mountains, 3D unit pawns/city houses/bulls), 4-angle rotation, pan/zoom.

## Implemented (latest)
- **Difficulty**: Setup selector — Peaceful / Easy / Normal / Hard. Peaceful bots prioritise trading and only fight defensively (attack when a player unit is adjacent/in range); Easy is timid; Normal balanced; Hard aggressive with bonus star income. Difficulty is stored on game state and drives `ai.ts`.
- **Trade-line tech pricing**: base costs set to Roads = 2, Construction = 5, Trading = 4, Trading Overseas = 15 (locked tech nodes now show their cost alongside the lock icon).
- **Quick Tutorial**: first-run 5-step overlay (Welcome → Move → Battle/Capture → Cities → Tech) with Next/Back/Skip and progress dots; auto-shows once (flag `hextribes_tutorial_seen_v1`) and re-openable from the pause menu → "How to Play".
- **Merchant UX fix**: units garrisoned on a city are now shown as a badge beside the city; tapping a city then tapping it again selects the garrisoned unit to move it out; Merchants are trained ready-to-move; single-tap a merchant = move options, double-tap a merchant = open its trade/inventory panel.
- **Unit move animation**: units glide tile→tile (~300ms); reachable/attackable tiles highlighted more boldly.
- **City level-up reward modal**: human cities enqueue a reward choice on level-up (Workshop / Treasury / Wall / Grand Park); AI auto-resolves.
- **Shareable victory card**: win/lose result renders a score card (tribe, turns, cities, techs, score) with a native Share button.
- **Naval & Ports (manual embark)**: build a Port on coastal water; land units embark → Rowing Boat → Sailing Boat → Battleship; auto-disembark on land.
- **Roads & Clear-Forest**: build **Roads** (needs Roads tech) on land — units chain along connected roads for free (fast travel); roads render as brown segments linking adjacent road tiles. **Clear Forest** (needs Construction) burns a forest tile into farmable grassland (adds a crop).
- **Merchants & Trading**: train a **Merchant** (needs Trading; **Merchant Ship** = embarked merchant, 8 slots vs 4 on land). Open its **Trade** panel to load goods and set a per-unit price (1–20★). Each round rival (bot) tribes buy your cargo — you earn the price in stars. You can also **buy from another player's merchant**: tap an enemy merchant to open its shop (BuyMerchantPanel) and choose **how many units** to buy per slot via a − / + / MAX stepper (capped by stock and what you can afford); the Buy button shows the live total cost, and purchasing pays stars to its owner. AI tribes with Trading train and stock a merchant. Merchants can't be attacked. **Sale notifications**: at the start of a turn, sales made by that player's own merchant that round are aggregated and shown in a `SaleModal` (only the merchant's owner is notified) — listing quantity sold per good and total stars earned (engine tracks `state.pendingSales` per player; cleared via `doClearSale`).
- **Peaceful capture**: tapping an *enemy city* opens a `CaptureModal` to buy it for **25 stars × its level** (discovered cities); a city can be bought **even if a military unit is garrisoned in it** — the purchase is peaceful, the occupying unit is **left untouched (not attacked or removed)**, and any of the previous owner's units standing **within that city's territory (its tile + neighbours) defect to the buyer** (`defectCityUnits`). When you buy a garrisoned city, a toast announces how many units joined your side. Tapping a *neutral village* offers an instant buy for **15 stars**. Buying transfers/founds the city immediately **for bot-owned cities** (bots implicitly accept and are paid the stars). **When you buy a city from another human player (pass-and-play)**, it instead sends an **offer**: your stars are escrowed and, on the seller's turn, they get an `OfferModal` showing how many stars they'd earn with **Accept** / **Decline** — accept transfers the city and pays the seller, decline refunds you (engine: `requestBuyCity` / `resolveCityOffer`, `state.pendingOffers`). The buyer sees a brief **"Offer sent"** toast confirming the deal is awaiting the seller's reply. Military capture (walking a unit in / defeating the defender) still works and is instant. (AI annexation only targets *undefended* cities.)
- **Village capture takes time**: moving a unit onto a neutral village now starts a *claim* (village tints to the claimer's colour) that only becomes a city at the start of the claimer's next turn, and only if a friendly unit still occupies it. Buying the village (15★) is the sole instant option.
- **Buildings & production**: tap a cell in your territory to build production structures that yield goods/stars each turn — Lumber Hut, Wheat/Bull/Horse Farm, Coal/Iron/Gold Mine. **Mountain ores**: each mountain rolls an ore on map-gen — 40% coal, 30% iron, 10% gold, 20% barren (no mine possible). A mine can only be built on a mountain carrying its matching ore (Coal Mine→coal / +2 pop, Iron Mine→iron ore / +2 iron/turn, Gold Mine→gold / +5 stars/turn); building the mine consumes the ore. Production credited at turn start; AI also builds. Buildings show as markers on the map.
- **Goods economy**: players hold wood/iron/wheat/meat/horses (second top-bar row). Unit training requires goods per the table; city chips show goods costs (red when short).
- Tribes: Lesnoi, Freemen, He-he, Fishmen; biome-specific procedural world-gen (Voronoi regions); map types Continents/Pangea/Lakes/Dryland/Archipelago.
- **New Game flow (screens)**: Main Menu → **Game Mode** (`app/mode.tsx`: centered *Single Player* above *Multiplayer Offline*). Single Player → **Create Game** (`setup.tsx`, vs AI) → Game. Multiplayer Offline → **Create Map** (`create-map.tsx`: map size, map kind, players 2–4, then *Next*) → **Choose Tribe** (`choose-tribe.tsx`, shown once per player in order; each picks a tribe then *Next*, last player taps *Start Game*) → pass-&-play Game. The number of tribes/regions on the map equals the number of players chosen; per-player tribes are passed to `generateGame` via `config.tribes` (duplicates allowed).
- **Game Kind (multiplayer)**: the Create Map screen offers **Open Game** vs **Closed Game**. In a **Closed Game** (`state.closed`): (1) a full-screen **Start-turn gate** shows the upcoming player's label + a centered **Start** button before each turn (so the previous player can't peek), and (2) **fog of war is per-player** — each player only sees the territory they've explored. Per-player exploration is tracked in `tile.seenBy`; `revealFor`/`applyFogForPlayer`/`refreshFog` in engine keep the visible `explored` flags in sync with the current player. Map sizes: Small 12×12, Normal 24×24, Large 36×36. Capitals always spawn with ≥5 land cells around them; neutral villages never spawn on mountains. **Mountain density is capped per tribe area** — the map is scanned in non-overlapping 3×3 chunks and surplus mountains are demoted to that chunk's dominant-tribe terrain: Lesnoi ≤1, Freemen ≤2, He-he ≤3, Fishmen ≤1 mountain per chunk (`MOUNTAIN_CAP` in mapgen).
- Branching radial tech tree (~29 techs). Units: warrior/archer/beefeater/catapult/rider/armored_rider/knight/pikeman/swordsman/merchant + naval tiers.
- **Seafaring AI**: coastal bots now prioritise the naval tech line (fishing→sailing), build a **port** bordering their land, **embark** units sitting on a port when their nearest objective is across water, and sail toward it (auto-disembark on landing) — so oceans and trade routes stay active.
- **AI annexation**: a wealthy bot will peacefully **buy a weak, undefended, nearby rival city** (level ≤ 2, keeping a stars buffer) each turn, adding late-game pressure.
- **Peaceful (economic) victory**: if the human comes to own **every city on the map** while at least one rival still has units (i.e. taken over by buying, not destroying), the game ends as a **"Capital Win"** — the human wins peacefully and no one is defeated (`state.peacefulWin`; VictoryCard shows the shared-win framing).
- **Claim progress ring**: a village being claimed shows a small hourglass ring in the claimer's colour ("captures next turn").
- **HUD**: the top pill shows your **star income per turn** (e.g. "+3"), while your **actual star balance** now sits in the resources panel alongside wood/iron/wheat/meat/horses (`starIncome()` in engine). Every time a city **reaches a new level it permanently produces +1 star per turn** (base bonus applied in `addPopulation`, independent of the chosen level-up reward). City panel opens even when a unit occupies it.
- **Combat defense bonuses** (multiply the defender's DEF, in `combat.ts` `defenseBonus`): open ground ×1; **forest ×1** normally, **×1.5 once Forest Care is researched**; **mountain ×1.25**. Inside your **own city**: basic units ×1 (×2 with a Wall); the **Knight** (defensive unit) ×2 (×4 with a Wall).
- **Roads**: a land unit **standing on a road tile moves twice as far** that turn (move ×2, in `grid.ts` `reachableTiles`); roads still carry units through forest/mountain without stopping. When a unit standing on a road is selected, the **extra tiles it can reach thanks to the road bonus are highlighted brighter** (lighter gold fill + white outline) to distinguish them from its normal move range (`roadExtra` in game.tsx / GameMap).
- **Draw / stalemate**: if the human still holds a city but has **no military units** and **every civilian (merchant) unit is boxed in** (all surrounding cells blocked by enemy units / impassable terrain), a 3-turn countdown starts. A gold HUD banner warns "Draw in N turns". If nothing changes for 3 of the human's turns the game ends in a **DRAW** (`state.status === "draw"`, shown on the VictoryCard). Any change — a freed cell, a newly trained military unit, or a civilian that can move again — resets the countdown and play continues. Logic: `isHumanStalemated` / `stalemateTurnsLeft` in `engine.ts`, evaluated at the start of each human turn in `startPlayerTurn`.
- **Fishing (fish tiles)**: tap a **fish** resource tile inside your own city borders (requires the **Fishing tech** and the tile must be discovered) to open a `FishChoiceModal` — **Hire a Fisherman** (2★, instant +1 pop to nearest city) or **Fish Yourself** (free 3D first-person fishing mini-game). Mini-game (`FishingMiniGame.tsx`, react-three-fiber + expo-gl, locked landscape): 3 worms; CAST the line, wait for the bobber to dip, then PULL within a 1.5s reaction window to catch (`fishSuccess` → +1 pop to nearest city). Missing all worms = no reward. Fishable tiles show the same soft pulsing golden ring as huntable bulls (`canFish` gate in engine; wired in `app/game.tsx` `doHireFisherman`/`doFishSuccess`). WebGL renders in preview but is best validated on a real device / Expo Go.
- **Hunting wild animals**: tap a wild animal (Bull) tile in your territory to open a choice modal — **Hire a Hunter** (3★, instant +1 pop & +1 meat to nearest city) or **Hunt Yourself** (free 3D first-person archery mini-game). Mini-game (`HuntingMiniGame.tsx`, react-three-fiber + expo-gl, locked landscape): player 12 HP / 12 arrows; bull charges & gores for 4 HP (with strafing dodges); hitboxes — head = 1-shot kill, body = 4 hits, legs = 12 hits. Killing the bull grants +1 pop & +1 meat to the nearest city; dying or running out of arrows = no reward. Hunting requires the **Hunting tech** and the animal must lie **inside your own city borders** (tapping bulls outside your territory or without the tech does nothing — enforced by `canHunt`). Huntable bulls display a soft **pulsing golden ring** on the map so players can see which ones are tappable. Wired in `app/game.tsx` (`doHireHunter` / `doHuntSuccess`). Note: WebGL renders in preview but is best validated on a real device / Expo Go.

- **Sound effects & settings toggles**: bundled offline SFX (`assets/sounds/`, generated by `scripts/gen_sfx.py` — no downloads) played via **expo-audio**: `tap` (UI selections), `battle` (attacks), `trade` (buying from a merchant + own-merchant sale notification), `coin` (city/village purchase via CaptureModal + city sale when an OfferModal is accepted). Manager in `src/utils/fx.ts` (`initFx` primes players at app start in `_layout.tsx`, `playSfx`, `haptic` wrapper). Three persisted settings in the **pause menu** — **Sound Effects** toggle, an **SFX volume slider** (`@react-native-community/slider`, 0–100%, master gain applied on top of each sfx's base volume, `hextribes_sfx_vol_v1`), and a **Vibration** toggle — stored in AsyncStorage (`hextribes_sound_on_v1` / `hextribes_haptics_on_v1`); the `haptic` wrapper gates every vibration call (game.tsx + both mini-games) so turning Vibration off silences all haptics. Reactive via `useFxSettings` (useSyncExternalStore).

- **3D unit models**: Warrior (`warrior`), Archer (`archer`), Swordsman (`swordsmen`), Rider (`rider`), Knight (`chivalry`), Pikeman (`pikemen`), Merchant (`merchant`), Catapult (`catapult`), Armored Rider (`armored_rider`) and Beefeater (`beefeater`) render on the map as pre-rendered low-poly **3D sprites** (from the user's `rider.obj` + FBX models) instead of the flat SVG pawn, each with its unit icon floating above. Sprites are tinted per tribe (`assets/images/<type>/…`). FBX models carry per-vertex colours (converted to glTF via `assimp`): already-coloured parts are kept, and the artist's **blank region — the default grey ~0.8 — is recoloured to the tribe colour** (`scripts/render_models.py`; `render_knight.py` / `render_rider.py` are the originals). All rendering is flat-shaded offline with matplotlib, so there's no runtime 3D cost. **All models are rendered at one shared world scale with a common feet baseline** (`scripts/render_all.py`); mounted units are additionally shrunk in 2D and re-grounded via the `SCALE` map (rider 0.5, armored_rider 0.5, chivalry/Knight 0.25) so they read at foot-unit size. Wired generically in `GameMap.tsx` via `MODEL_SPRITES` (keyed by unit type) + `modelSprite()`; the SVG pawn is skipped for any type in `MODEL_SPRITES`, and an `<Image>` is drawn in both the static token overlay and the glide/anim token. Embarked units (with `unit.boat`) still show the boat, not the model.

- **Peaceful difficulty is truly peaceful**: peaceful bots never strike first. `Player.provoked` (types.ts) is set on a defender's owner inside `engine.attackUnit` whenever a unit is attacked by a different owner; `ai.ts` only fights when `cfg.aggressive || provoked` (peaceful has `aggressive:false`), so a peaceful bot stays passive until the human attacks one of its units, then retaliates. Covered by `scripts/engine_test.js` (77/77): "peaceful bot does NOT strike first", "attacking a peaceful bot provokes it", "provoked peaceful bot fights back". Verified by testing_agent (iteration_7).

- **Expo SDK upgraded 54 → 57** (expo 57.0.19, react-native 0.86.3; `expo-doctor` 20/20). Breaking changes handled: removed `newArchEnabled`/`edgeToEdgeEnabled` from `app.json`; migrated icons from `@expo/vector-icons` (`import { MaterialCommunityIcons }`) to `@react-native-vector-icons/material-design-icons` (default import, same local name, identical MDI glyph names) across all 26 screens/components; added its config plugin (auto). The old Expo-Go CDN font hack (`use-icon-fonts.ts` + splash gate in `_layout.tsx`) was removed — `@react-native-vector-icons/common` now auto-loads the bundled `MaterialDesignIcons.ttf` at runtime via ExpoFontLoader (works in Expo Go). Added missing peer dep `expo-asset`. Verified: icons render in menu + game, engine tests 77/77.
- **Deployment readiness (health check PASS)**: fixed a compile blocker — `src/game/types.ts` `Player` interface was missing its closing brace (from an earlier `provoked` edit); added it. Added backend health endpoints in `server.py`: top-level `GET /health` (direct k8s probe on :8001) and `GET /api/health` (via ingress), both `{status:ok}` 200. Removed `.env`/`.env.*`/`*.env` from root `.gitignore` so env files aren't excluded. deployment_agent = PASS (findings: []); verified by testing_agent (iteration_9): `/api/health` 200, frontend boots + turns advance with no crash.
- **Crash fix — "Cannot read properties of undefined (reading 'wood')" on loading an old save**: `continueGame` in `store.tsx` now normalizes every player's `goods` to a complete record (`{wood,iron,wheat,meat,horse}` defaulted to 0) and backfills `provoked`, so older/incompatible saves no longer crash the HUD (`player.goods['wood']`) at render. Reproduced (stripped goods from a real save → previously crashed) and verified fixed via Continue.
- **Post-upgrade fix #3 (the actual native fix) — duplicate three.js on native**: web was fixed by the per-specifier resolver, but Android Expo Go logs still showed many "Multiple instances of Three.js" (web logs did not). Cause: Metro **package-exports** (ON by default since SDK 55; it was OFF in SDK 54 where the mini-games worked) resolved `three` via both its ESM (`three.module.js`) and CJS (`three.cjs`) entries on native → two instances → r3f renderer drew nothing. Fix in `metro.config.js`: `config.resolver.unstable_enablePackageExports = false` (restores SDK-54 single-`main` resolution → `three.cjs` on all platforms), kept alongside the `resolveRequest` three→single-file shim. After this, fresh bundles log ZERO "Multiple instances"; app + game + both mini-games still render on web; engine 77/77; `collapsable={false}` also retained on both mini-game roots. (Web verified; native pending user reload.)
- **Post-upgrade fix #2 — mini-games blank on NATIVE only (Expo Go)**: web worked after the three fix but Android Expo Go still showed a blank Canvas. Cause: under the New Architecture (default since SDK 55, RN 0.86), view flattening unmounts/remounts the `expo-gl` GLView during the mini-games' frequent re-renders (joystick/HP state), killing the GL context. Fix: added `collapsable={false}` to the root `<View>` wrapping the `<Canvas>` in both `HuntingMiniGame.tsx` and `FishingMiniGame.tsx` (ref: pmndrs/react-three-fiber, expo/expo#37725). `collapsable` is a native-only prop so web is unchanged. (Verified on web = no regression; native verification pending on user's device.)
- **Post-upgrade fix — 3D mini-games blank WebGL Canvas (web)**: after the SDK 57 upgrade the `@react-three/fiber` mini-games rendered nothing (map showed through). Cause: Metro package-exports (default since SDK 55) resolved `three` via two entry files → "Multiple instances of Three.js" → the WebGLRenderer produced no output. Fix in `metro.config.js`: a `resolver.resolveRequest` shim forces every bare `three` import to a single file (`node_modules/three/build/three.cjs`). Verified by testing_agent (iteration_8): both mini-games render their 3D scenes; general post-upgrade smoke passes. (Note: End Turn testID is `action-end-turn`.)

- **Reverted Expo SDK 57 → 54 (user request)**: SDK 57 mandates the New Architecture (cannot be disabled), under which `expo-gl` + `@react-three/fiber` WebGL fails to render on real devices — the Hunting/Fishing mini-games showed a blank canvas (map visible behind the SHOOT overlay). SDK 54 is the last version supporting the arch/metro combo where the mini-games worked. Reverted the 32 upgrade-touched files + `metro.config.js` to pre-upgrade commit `7f2c838` (expo 54.0.35, react-native 0.81.5, expo-gl 16.0.10, `@expo/vector-icons` 15.1.1 restored, old `use-icon-fonts` font hack + splash gate restored, `newArchEnabled: true` kept because reanimated 4.1.1 requires it). KEPT the later store.tsx save-crash fix and server.py health endpoints. Pinned `electron-to-chromium` to `1.5.420` in resolutions (1.5.421 tarball 404s on the registry). Verified: web bundle compiles (200), app boots to menu with icons, engine 77/77. Mini-game WebGL must be validated on a fresh native build (works on web preview regardless of SDK). `app/game.tsx` mounts `HuntingMiniGame`/`FishingMiniGame` inline while `huntPlaying`/`fishPlaying`.

- **City territory expansion (purchasable rings)** — default city territory is the 3×3 (ring 0+1). Players & AI buy outward concentric rings by Chebyshev distance: Tier 2 (dist 2)=5★/cell, Tier 3 (dist 3)=10★/cell, Tier 4 (dist 4)=20★/cell. A tier unlocks only when the previous ring is fully bought (off-map / rival-controlled cells don't gate). Bought cells (`city.expandedTiles: number[]`) become full territory (build/harvest/road). Engine: `TERRITORY_TIER_COST`, `expansionOptionForTile`, `canExpandTerritory`, `expandTerritory` in `engine.ts`; `cityTerritory` now includes `expandedTiles`. Store: `doExpandTerritory(tileId)`. UI: no map highlight — tapping an empty tile just outside current borders opens `ExpandModal` (testID `expand-modal`, buy=`expand-buy`, cancel=`expand-cancel`); `game.tsx` tap handler falls through to `expansionOptionForTile` when no other action applies, and the `territory` memo includes expanded tiles. AI (`ai.ts` step 3f): a bot with spare stars (buffer 8) buys one cheapest available expansion tile per turn. Verified: 7 new headless tests (engine_test.js now 84/84) + testing_agent iteration_10 (buy deducts −5, tile joins territory, tier-3 gating, cancel, end-turn regression all PASS).

- **Build panel opens even when unaffordable (red price)** — Previously `tileHasActions` filtered out star-gated options, so tapping a tile you couldn't afford anything on did nothing. Now `canBuild`/`canInfra` accept `{ ignoreStars?: boolean }`; `tileHasActions` uses `ignoreStars:true` so the Build panel opens whenever a tile is valid in every way except the star cost. In `BuildPanel.tsx`, an option that is valid-except-stars (`onlyStars`) is shown at full opacity with a **red** price + red star icon + red border (`chipUnaffordable`, `costRed`) and stays press-disabled; options failing for other reasons (tech/terrain/occupied) keep the dimmed/lock styling. AI `buildableFor` stays strict (bots never attempt unaffordable builds). Verified by 5 new headless tests (engine_test.js now 90/90).

- **Production build fix — three.js static class blocks** — EAS `expo export` (production bundle for iOS/Android, `--no-bytecode`, runs on Hermes) failed with `SyntaxError: three.core.js: Static class blocks are not enabled. Please add @babel/plugin-transform-class-static-block`. Root cause: `three` (pulled in by the react-three-fiber mini-games) ships modern `static { }` class blocks which Hermes can't run untransformed, and the project had NO `babel.config.js` (dev preview used Expo's internal default so it never surfaced). Fix: created `/app/frontend/babel.config.js` with `presets: ['babel-preset-expo']` (preserves reanimated 4/worklets auto-plugin) + `plugins: ['@babel/plugin-transform-class-static-block']`, and added `@babel/plugin-transform-class-static-block@7.29.7` to devDependencies. Verified by running `npx expo export` locally for BOTH android and ios — both export a 5.34 MB bundle with zero errors. Dev preview still boots. No docker/config changes.

- **Amendment: Rider re-render (selective tribe tint)** — user uploaded an updated `rider.fbx` whose vertices are painted (horse brown/tan/dark) except the tribe region left blank `[0.8,0.8,0.8]` grey. Converted FBX→glTF with `assimp export` (installed `assimp-utils`), and rewrote `scripts/render_rider.py` (now gltf-based) to render only the Rider per tribe, tinting ONLY blank (~0.8) vertices (`is_blank`) and preserving all painted colors. Sprites re-anchored to the EXACT existing footprint (canvas 213×175, content height 77px, baseline y=165) so on-map size is unchanged. Overwrote `assets/images/rider/rider_{nature,desert,volcanic,snow}.png`.
- **Amendment: Economy panel (#2)** — added a chart button to the goods HUD (`TopHUD` testID `hud-economy-btn`) opening `EconomyModal` (testID `economy-modal`). Shows for the current player: (1) income per turn (stars via `starIncome` + each good via new `engine.goodsIncome`), (2) Bought-from-merchants table (Qty / Avg★ / Spent★), (3) Sold-via-merchants table (Qty / Avg★ / Income★). Tracking: added `Player.economy = { bought, sold }` ledger (types.ts `GoodLedger`), `recordPurchase` (wired into `buyFromMerchant` + bot `resolveTrades`) and extended `recordSale` to log sales for ALL players. Initialized in `mapgen.ts` and normalized on save-load in `store.tsx`.
- **Amendment: fog-gated buying (#3)** — cities/villages could be bought on undiscovered tiles by non-human players (buy checks were hardcoded to `player === 0 && !tile.explored`). Added `engine.hasDiscovered(state, player, tileId)` (closed games → per-player `seenBy`; open games → human 0 uses `explored`, bots omniscient) and used it in `canBuyVillage`/`canBuyCity` for ALL players and bots. Verified: engine_test.js now 95/95 (incl. undiscovered-village-blocked-in-closed-game + economy ledger + goodsIncome), and testing_agent iteration_11 (Economy modal 10/10 UI checks, end-turn regression) all PASS.

- **Fog buy bug STILL reproduced (screen recording) — real fix** — the earlier engine-only fix wasn't enough: `app/game.tsx` `onTileTap` had its OWN pre-checks that bypassed the engine. It opened the CaptureModal whenever `canBuyCity(...).price > 0` (ignored the `.ok`/discovery result) and used the old `(cp !== 0 || tile.explored)` for villages. Fixed both to gate on `hasDiscovered(state, cp, tile)` so tapping a fogged city/village no longer opens the buy modal for any player/bot. Verified: engine_test.js 97/97 (added undiscovered-enemy-city block test) + UI smoke test (no capture-modal on fogged taps).

- **"Sold table empty" report — investigation** — could NOT reproduce a code bug. Verified the full sold pipeline: added engine tests for (a) a bot buying from the human's merchant via `resolveTrades`, (b) the full `advanceTurn` round market tick, and (c) pass-and-play `buyFromMerchant` recording the owner's SOLD + buyer's BOUGHT ledgers — engine_test.js now 102/102. Also temporarily seeded `player0.economy` and screenshotted the live Economy panel: both Bought (Wood 4 / avg 3.0 / spent 12) and Sold (Meat 3 / avg 3.0 / income 9) render correctly (seed then reverted). Conclusion: SOLD only counts COMPLETED sales made AFTER the feature shipped (a rival actually buying your stock); listing stock or pre-feature sales don't count. Pending user confirmation of their exact repro (whether they saw the "You sold X" popup).

- **"Sold empty" — verified working, not a code bug** — user saw the "You sold X" popup but empty Sold table (pass-and-play). Proved `pendingSales` (the popup source) is written ONLY inside `recordSale`, AFTER the `economy.sold` update — so no code path shows the popup without filling the ledger. Confirmed LIVE in single-player: temporarily seeded a stocked human merchant + rich bots, the turn-1 market tick sold 1 wood, dismissed the "Merchant Sale!" popup, opened the Economy panel and the Sold table showed Wood Qty 1 / Avg 1.0 / Income 1 (seed reverted). Pass-and-play buy path (`BuyMerchantPanel → doBuyFromMerchant → buyFromMerchant → recordSale`) also records the owner's Sold ledger (engine test). Conclusion: the user's popup was from a sale made under the PREVIOUS version (its `pendingSales` persisted in the ongoing save) — old sales aren't counted retroactively; new sales are. Fix on user side: fully reload Expo Go / start a fresh game. engine_test.js 102/102.

- **"Sold empty" — ROOT CAUSE found & fixed (layout, not data)** — 2nd recording (pass-and-play) showed the "Merchant Sale" popup (Iron ×4 +32★) but the Sold table appeared empty. The data WAS recorded correctly (proven); the real issue was the EconomyModal's 3 tall sections overflowed the sheet (`maxHeight: 82%`) with the scroll indicator OFF, so the Sold rows sat below the fold and users couldn't tell to scroll. Fix in `EconomyModal.tsx`: sheet `maxHeight` 82%→90%, restructured (sheet is now a `View`, header wrapped in a no-op `Pressable`, `ScrollView` a flex sibling with `showsVerticalScrollIndicator`/`persistentScrollbar`/`nestedScrollEnabled` for reliable touch scrolling), and compacted rows (paddingVertical 9→6, card paddingVertical→2, section marginTop md→sm). Result: all three sections (Income / Bought / Sold) now fit on one phone screen — verified live, Sold showed Iron Qty 1 / Avg 8.0 / Income 8. engine_test.js 102/102.

- **Rider 2× bigger + transparent bg + one-finger economy scroll** — (1) Rider had a white backing box on the map; root cause was the render fig/axes background. Updated `scripts/render_rider.py` to set `fig.patch`/`ax.patch` alpha 0 (+ transparent panes) so the PNG background is fully transparent (verified: bg pixels RGBA 0,0,0,0; only horse/rider opaque). (2) Made the Rider 2× bigger by doubling `CONTENT_H` 77→154 on the same 213×175 canvas & baseline (feet stay grounded; all model sprites share a fixed 80×66 `contain` box in GameMap, so a 2× content fraction = 2× on-map). Re-ran assimp FBX→glTF + render for all 4 tribes. (3) Economy panel needed two fingers to scroll because the map uses `react-native-gesture-handler`; switched `EconomyModal` to RNGH `ScrollView` wrapped in `GestureHandlerRootView` (native-only fix — verify single-finger scroll on a device build). Modal still renders all 3 sections; lint clean; engine_test 102/102 (unchanged).

- **7 amendments (rider size, scroll, mine pop, icons, motto, name, tutorials)** — (1) Rider 2× SMALLER: `render_rider.py` CONTENT_H 154→77, re-rendered 4 tribes (transparent bg kept). (2) Economy one-finger scroll: restructured `EconomyModal` so the close-backdrop is an `absoluteFill` Pressable SIBLING *behind* the sheet (no longer an ancestor of the ScrollView) + RNGH `ScrollView` inside `GestureHandlerRootView` — native single-finger scroll (verify on device). Verified opens/closes-on-outside-tap on web. (3) Iron-mine population: confirmed NOT a code bug — `iron_mine`/`gold_mine` aren't in `BUILDING_POP` so they add 0 population (only `coal_mine` +2, per its tech desc); added engine tests (104/104). (4) Icon consistency: mine tech + building icons now match the ore icon on the mountain — coal=`fire`, iron=`anvil` (was pickaxe/gold), gold=`gold` (was cash-multiple); `mining` tech icon `diamond-stone`→`fire`. (5) Motto → "Trade. Buy. Grow. Thrive." (index.tsx). (6) Name "HexTribes"→"Poly World" (index.tsx title, VictoryCard brand + share title, app.json display `name`). (7) Tutorials updated (`TutorialOverlay` + `how-to-play`): new name/motto, added a Trade & Economy step, mine-icon note, trade-focused goal.

## Engine verification
- **City Screen unique building models** — Rendered the 5 user-provided FBX models to 2D isometric sprites via `scripts/render_city_buildings.py` (assimp FBX→glTF, matplotlib ortho elev=30/azim=-45; outputs `assets/images/city/{sawmill,stone_quarry,sand_quarry,glass_factory,trade_tower}_tm.png`). Wired into `CityMap.tsx`: the **Material Factory** now shows a **unique per-tribe** building sprite (nature=sawmill, volcanic=stone_quarry, desert=sand_quarry, snow=glass_factory) and the **Trade Tower** shows the skyscraper sprite — replacing the old grey SVG box. Sprites are bottom-anchored to their footprint via hardcoded `SPRITE_ASPECT`/`SPRITE_SCALE` (Image.resolveAssetSource crashed on web — removed). Move-drag & factory-config tap now hit-test the sprite's on-screen rectangle so tall sprites are grabbable/tappable anywhere. `CityMap` gained a required `tribe` prop (from `city.tsx` player.tribe). Verified: `node scripts/engine_test.js` **168/168** + testing_agent iteration_21 (City Screen loads with NO resolveAssetSource crash; factory renders as tribe building sprite; tap→dialog, move, demolish, end-turn all PASS) + 4-tribe screenshot check.

## Engine verification
- **3 amendments: coal-mine icon, Economy all-resources, peaceful-bot rework** — (1) **Coal Mine icon**: pickaxe interior filled **white** (was transparent) keeping the black outline + black coal lump (`assets/images/coal_mine.png` edited via PIL flood-fill of enclosed transparent holes). (2) **Economy "Per turn"** table now lists **every resource** (Stars + all 10 goods incl. Coal) even when income/cost is 0 (`EconomyModal.flowGoods = CITY_GOODS`). (3) **Peaceful-difficulty bot AI** (`ai.ts`): until the human attacks a bot (`provoked=false`) it is **restrained** — militia capped at exactly **1 catapult + 1 rider + 3 warriors** (no other military), **no annexation**, and units only step off their own city tile into home territory (never roam to grab villages / surround the player). Restrained bots secure a **Merchant first**, run a tribe "factory" (+4 of `TRIBE_MATERIAL[tribe]`/turn) and stock that material **first** so the human can buy what their own tribe can't make. Being attacked lifts all restraints (normal AI, fights back). Verified: `node scripts/engine_test.js` **168/168** (militia caps, tribe-material selling, provoked lifts caps) + testing_agent iteration_20 (Peaceful game starts via `difficulty-peaceful`, End Turn ×6 T1→T7 no crash, Economy lists all 11 rows incl. Coal) + coal PNG visual check.

## Engine verification
- **Trade materials + coal icon + Economy Income/Costs/Profit** — (1) Merchants & Merchant Ships can now carry/trade the crafted materials **planks, stone, sand, glass** (new `TRADE_GOODS = base 5 + those 4`, coal excluded). `MerchantPanel` empty-slot picker lists all 9 (wraps to 2 rows); `BuyMerchantPanel`, `SaleModal` and the Economy Bought/Sold history resolve via `CITY_GOODS`/`TRADE_GOODS`. (2) **Coal Mine** chip/map icon background changed from near-black `#3A3A3A` → grey `#6E747B` (`BUILDINGS.coal_mine.color`). (3) Economy panel **"Per turn"** section is now a 3-column table **Income | Costs | Profit** (Profit = Income − Costs), driven by new `engine.economyProjection()` which clones state, applies a turn's building income, replays `runCityFactories`, and diffs goods to split gains (income) vs spends (costs). Added an `if (!visible) return null` guard to `EconomyModal`. Verified: `node scripts/engine_test.js` **162/162** + testing_agent iteration_19 (3-column table with live factory Wood −6 / Planks +3, Stars +1/0/+1; caught & fixed a `GOODS` import crash) + screenshot.

## Engine verification
- **City Screen polish (user video feedback)** — (1) **Houses** were oversized: shrank the house sprite to exactly match its 2×2 footprint (removed the 1.18 over-scale). (2) **Material Factory** now renders as a real isometric 3D building block (grey roof + two shaded side faces via `factoryBox` in `CityMap.tsx`) with the icon on its roof, instead of a flat grey slab. (3) **Move Building rewritten**: the old tap-to-select flow lost the touch to the map pan gesture (buildings never picked up). Move is now a single press-and-drag on a dedicated `move-layer` — `buildingAt` hit-tests the building under the finger (probing downward by `FACTORY_H` to catch grabs on the elevated factory roof/icon), `moveUpdate` tracks a green/red ghost with the same elevation offset, and release commits via `moveCityBuilding`. **Demolish refund**: `demolishCityBuilding` returns the full build cost (stars + goods). Verified: `node scripts/engine_test.js` **158/158** + testing_agent iterations 17→18 (roof-grab & base-grab move, invalid-drop guard, 3D factory, demolish/roads regressions — all PASS).
## Engine verification — (1) House sprites are now **bottom-anchored** to their footprint diamond (aspect-correct, base = front-bottom vertex) so they no longer float above the ground (`CityMap.tsx` house render). (2) The bottom bar gains an **Edit** button (`city-edit`) between Roads and Exit opening an `edit-menu` with three tools: **Move Building** (`edit-move` — select a building, drag it to a new valid cell; `moveCityBuilding` ignores the building's own footprint), **Demolish** (`edit-demolish` — tap any building except the citadel to remove it; `demolishCityBuilding`), **Delete Road** (`edit-delete-road` — roads tint red, tap a cell to remove it; `removeCityRoad`). Store: `doMoveCityBuilding`/`doDemolishCityBuilding`/`doRemoveCityRoad`; `canPlaceCityBuilding` gained an `ignoreId` param for in-place moves. Verified: `node scripts/engine_test.js` **157/157** + testing_agent iteration_16 (all 10 flows — place/move/demolish/delete-road, bar layout, edit toggle, end-turn regression — PASS).
## Engine verification
- **Growing City — Phase 3 (Roads) + Phase 4 (Factories & Trade Tower)** — City Screen now supports drawing cosmetic roads by dragging a finger (each cell = 1 road; `road-layer` gesture in `CityMap`, `canPlaceCityRoad`/`drawCityRoads` in engine, `doDrawCityRoads` store; roads blocked under the citadel/buildings/existing roads). Tribe **Material Factories** run per-city each turn (`runCityFactories` in `startPlayerTurn`): Lesnoi=planks (feed wood via a tap-to-open stepper, 2 wood→1 plank; `setFactoryFeed`/`doSetFactoryFeed`, stored on `CityBuilding.feed`), He-he=+2 stone/turn, Freemen=+5 sand/turn, Fishmen=+1 glass/turn (costs 5 sand + [1 coal OR 2 wood], prefers coal; starves + shows a ⚠ badge/warning when short via `CityBuilding.starved`). Tapping a placed factory opens `factory-dialog` (`FactoryDialog` in city.tsx) showing its rule/status. **Trade Tower** (`hasTradeTower`/`tradeMultiplier`) doubles a player's trade income in both `resolveTrades` (market tick) and `buyFromMerchant` (pass-and-play). Verified: `node scripts/engine_test.js` **149/149** (road validation, all 4 tribe factories incl. glass starve/fallback, feed cap, trade-tower 2x) + testing_agent iteration_15 (roads draw+persist, free factory places GREEN, factory dialog + feed stepper, citadel/exit, end-turn regression — all PASS).
## Engine verification
- **Growing City — Phase 1 (City Screen scaffold)** — CityPanel gains a "City Actions" section with an **Enter the City** button (`enter-city`) that navigates to `app/city.tsx`. The City Screen has: `CityMap` (30×30 isometric grass, pan/pinch) with a centered 6×6 **Citadel** whose model is chosen by `citadelStage` (1/5/10/15 → `assets/images/city/citadel_*_tm.png`, rendered by `scripts/render_city.py`); a top HUD (city ⭐ income + player stars & all goods incl. new **planks/stone/sand/glass/coal** via `CITY_GOODS`); a bottom bar **Citadel / Buildings / Roads / Exit**. **Citadel** upgrade works (engine `canUpgradeCitadel`/`upgradeCitadel`, costs 10★+50 planks→5, 30★+150 stone+25 planks→10, 40★+100 stone+10 glass+40 planks→15; store `doUpgradeCitadel`). Buildings sheet lists House 2×2 / Factory 3×3 / Trade Tower 4×4 / Park; placement + Roads + factory production are Phase 2–4 (currently "coming soon" toasts). New `GoodType`s added (planks/stone/sand/glass/coal, global per-player), `City.citadelStage` + `City.layout` added + migrated + seeded in `factory.newCity`. Verified: engine 124/124; City Screen renders live (citadel + HUD + bar).
- **Engine verification**
- **Save slots (10) — Saved Games screen + in-game Save** — main menu now has a `Saved Games` button (`menu-saved-games`) between Continue and New Game opening `app/saved-games.tsx`, which lists 10 slots (`slot-0..9`) via the reusable `SaveSlotList` component. The pause menu has a `Save Game` button (`menu-save`) opening a bottom-sheet slot picker; tapping a slot writes the current game (with metadata) and toasts "Saved to Slot N". Store (`store.tsx`) adds `saveToSlot`/`loadFromSlot`/`listSlots` + `SlotInfo`, keys `hextribes_slot_0..9` = JSON `{savedAt, state}`; save migration extracted into shared `migrateState()` (reused by continueGame + loadFromSlot); loading a slot also refreshes the Continue save. Verified end-to-end by testing agent (all 5 criteria pass, iteration_12).
- **3D Merchant Ship** — embarked merchants now render a dedicated 3D model (`scripts/render_merchant.py` → `assets/images/merchant_ship/merchant_ship_<tribe>.png`), blank sails tinted per tribe. `GameMap` adds `MERCHANT_SPRITES` + `merchantSprite()` and `BOAT_SIZE.merchant`; both the static token and the move animation pick the merchant sprite/size when `unit.type === "merchant"` while embarked (other boats unchanged). Verified on the map.
- **3D boat sprites (Rowing / Sailing / Frigate)** — replaced the SVG-drawn boats with rendered 2D sprites from the user's FBX models. `scripts/render_boats.py` converts each FBX (via `assimp export` → glTF) and renders per-tribe PNGs at a shared world scale with a shared union crop (uniform placement + correct relative sizes); blank ~0.8 grey vertex regions (sails/painted parts) are recoloured to the tribe colour, all other vertex colours preserved; orientation normalised so hull length runs along X for every model. Assets in `assets/images/{rowing,sailing,battleship}/<tier>_<tribe>.png`. `GameMap.tsx` adds `BOAT_SPRITES` (keyed by naval tier) + `boatSprite()`; boat units and move animations now render the sprite (90×92 overlay, hull at the waterline) instead of `drawBoat` (SVG kept only as a fallback). Merchant Ships reuse the sailing sprite.
- **Engine verification**
- **Custom iron icons (P0 amendment)** — added a reusable `GameIcon` component (`src/components/GameIcon.tsx`) that renders a bundled PNG for `img:*` icon keys and falls back to `MaterialCommunityIcons` for normal glyph names. Wired the user's uploads: `iron_ingot.png` (`img:ingot`) now represents the **iron good, the iron_ore map resource badge, and the Iron Mine tech node**; `iron_mine_icon.png` (`img:mine`) is the **Iron Mine building icon**. The ingot art was recolored to a steel tone + cropped to content so it reads on both light (tech tree / resource badge / economy) and dark (HUD) surfaces. Swapped the icon render at every data-driven site: TopHUD goods row, GameMap building & resource markers, BuildPanel (building chip + produce list), TechTreeModal, MerchantPanel, BuyMerchantPanel, CityPanel goods-cost, SaleModal, EconomyModal. Verified live: ingot shows in the HUD iron slot and as the mountain ore badge. engine 115/115, lint clean.
- **Dual-resource Port cost + split Main/Trade Ports (P0 amendment)** — (1) Infrastructure now supports a `woodCost` alongside stars: `canInfra`/`doInfra` in `engine.ts` require AND deduct wood; `BuildPanel` shows the wood price with a tree icon (red when short). (2) **Main Port** (`port`, tech *Sailing*, 10★ + 12 wood) embarks all military/civilian units EXCEPT merchants → Rowing Boat. (3) **Trade Port** (`trade_port`, tech *Trading Overseas*, 8★ + 10 wood) embarks ONLY merchants, straight to a **Merchant Ship** (boat set to `sailing`, 8 slots, no boat upgrades) — merchants skip the rowing-boat tier. New `Tile.tradePort` flag (normalized on save-load in `store.tsx`, defaulted in `mapgen.ts`). `grid.canEnter` routes merchants onto trade-port water tiles and other units onto main-port tiles. Trade Port renders a green-flagged dock in `GameMap`. `UnitPanel` labels an embarked merchant "Merchant Ship" (ferry icon) and hides the boat-upgrade button. Verified: `node scripts/engine_test.js` **115/115** (wood-cost blocks build, panel opens via ignoreStars, exact star/wood deduction, merchant blocked on main port, warrior blocked on trade port, merchant→sailing ship, iron mine +2 pop & +2 iron).
- `frontend/scripts/engine_test.js` — headless assertions pass (merchant train/load/price/trade, road build, burn-forest, port build, embark, boat upgrades & stats, water blocking, road reachability). Run: `node scripts/engine_test.js`.

## PHASED / NOT YET BUILT (from instruction.docx — large systems)
- Goods PRODUCTION: buildings that generate wood/iron/wheat/meat/horses each turn (goods are spent already; production is next).
- Buildings: Lumber Hut, Windmill, Forge, Coal/Iron/Gold Mines, Wheat/Bull/Horse Farms, Temple; tap-cell-to-build flow; per-turn production; 80% of grass = farmable land.
- Roads (build + movement bonus), burn-forest-to-farmland.
- Naval: Rowing Boat (auto-convert on port), Sailing Boat upgrade, Battleship; ports & trade ports.
- Merchant & Merchant Ship trading: 4/8 inventory slots, load goods, set prices, trade sign for other players; bots buy-only, can't attack merchants.
- Ore-site research (coal/iron/gold) revealing mine sites.

## Backlog / Next
- Build the goods economy + resource top-bar (foundation for buildings, unit costs, trading).
- Then buildings & per-turn production, then naval + upgrades, then merchant trading UI.
